---
id: E14-S3-session-event-linkage-rekey
epic: E14-SR-session-replay
status: ready-for-dev
area: session-replay
touches: [browser]
depends_on: [E14-S2]
api_impact: additive
---

# E14-S3-session-event-linkage-rekey — Session/event linkage + re-key on rotation

## Why

A recording is only useful if it stitches to the captured events of the same session. This story makes the recorder read the **same** `SessionIdManager` the capture path uses (it does NOT mint its own id), so the recording and the events resolve to one session id — and re-keys the recording when the session id rotates (idle/max-length expiry mid-recording). This is the one hard correctness invariant of the epic: without it, recording and events don't join, and `getReplayId()` returns a fabricated id.

## Scope

### In

- **The recorder reads the shared `SessionIdManager`** (`ts/packages/browser/src/session-id-manager.ts`) — the SAME instance the browser adapter uses to stamp `NeutralEvent.sessionId` (E4-S8). It does NOT construct its own `SessionIdManager` and does NOT mint its own id. `getReplayId()` returns that shared session id (replacing S2's placeholder), as an opaque neutral string.
- **Re-key on session-id rotation** — when the session id rotates (idle/max-length expiry, or reset) mid-recording, the recorder observes the rotation and starts a new recording segment keyed to the new id, so stitching is preserved across the boundary.
  - The pure `SessionIdManager` has no `onSessionId` observer (verified: `checkAndGetSessionId(timestamp)` at `session-id-manager.ts:51` returns the current/fresh id; the browser adapter already detects rotation by comparing the freshly-stamped id against the last one seen — `classifySessionTransition` at `browser-adapter.ts:645-650` returns `'adopted'|'rotated'|'same'`, computed at `:585`, committed at `:589`). **Reuse that existing adapter-side rotation verdict** — do NOT add a second rotation detector, and do NOT add an `onSessionId` observer to the pure `SessionIdManager`.
  - **The exact hook (pinned — refine + architect 2026-07-10):** the verdict is `private` and there is NO public `getSessionId()` and NO listener mechanism on the adapter today, so "reuse the verdict" requires exposing ONE additive fan-out — NOT a second detector. Add a public `onSessionRotated(listener: (sessionId: string) => void): () => void` on `BrowserAdapter` (near `getDistinctId()`, `:903`) that: (a) registers the listener, (b) **primes it immediately with `this.lastSeenSessionId` on subscribe** (mirrors the reference `onSessionId`'s subscribe-time current-value fire — this is how the recorder keys its FIRST segment at `start()`, so no separate public session-id read is needed), and (c) returns an unsubscribe closure (the recorder disposes it on `stop()`/teardown). Fire the listeners from the EXISTING verdict site: right after `commitSessionTransition` (`:589`), `if (transition === 'rotated' && stamped.sessionId !== undefined) this.notifySessionRotated(stamped.sessionId)`. This is one verdict, one more consumer (peer to `trackPageview`/`maintainSessionEntry`) — NOT a second detector, NOT a `checkAndGetSessionId` passthrough (which would be a second way to advance the session). The `SessionIdManager` stays untouched.
- **`getReplayId()` returns the neutral id, never internals** — never the persisted `SessionTuple`, the `SESSION_ID_KEY` storage key, or a `/replay/{id}` URL. Just the opaque session id string, or `undefined` when inactive.
- **Assert the linkage invariant** — a test proving the recording's session id equals the id stamped on `NeutralEvent.sessionId` for events captured in the same session, and that after a forced rotation the recorder re-keys to the new id (both segments' ids match the events captured in their respective windows).

### Out

- **Snapshot buffering + delivery** — S4. This story wires the id linkage + rotation re-key; S4 owns the buffer, flush cadence, and delivery path (including the **re-decide-on-rotation sampling guard**, which builds on this story's rotation hook).
- **The sampling DECISION mechanism** — S4. This story exposes the rotation signal the sampling guard re-decides on; the decision + flush-guard live in S4.
- **Adding an `onSessionId` observer to `SessionIdManager`** — explicitly out; reuse the adapter's existing rotation verdict via the additive `onSessionRotated` fan-out (`browser-adapter.ts:585-589` / `:645-650`).
- **Closing the capture-gated cadence gap** — S3 pins the hook and DOCUMENTS the gap (see Technical notes); whether to add an idle-timer tick (landing 2) is a user-input decision surfaced at refine, not silently built into S3. S3 ships the fan-off-the-verdict mechanism regardless.
- **Any seam change** — the port is frozen (S1).

## Acceptance criteria

- [ ] The recorder reads the shared `SessionIdManager` instance (the same one the capture path stamps `NeutralEvent.sessionId` from) — it mints NO id of its own. A test asserts the recording's session id equals the events' `sessionId` in the same session.
- [ ] On session-id rotation mid-recording, the recorder re-keys to the new id (starts a fresh segment) — a test forces rotation (idle/max-length/reset) and asserts the post-rotation recording id matches the post-rotation events' `sessionId`.
- [ ] The rotation re-key reuses the browser adapter's existing rotation verdict via an additive `onSessionRotated(listener)` fan-out fired from the verdict site (`browser-adapter.ts:585-589`, on `transition === 'rotated'`), priming the listener with `lastSeenSessionId` on subscribe — NO second rotation detector, NO public `getSessionId()` passthrough, NO `onSessionId` added to the pure `SessionIdManager`.
- [ ] `getReplayId()` returns the opaque neutral session id (or `undefined` when inactive) — never the persisted tuple, the storage key, or a URL.
- [ ] Gates green: `cd ts && pnpm turbo run build test typecheck lint` + `pnpm neutrality-scan`.

## Technical notes

- **The shared session id IS the join key** — architect-locked (epic Notes → "Session/event linkage", 2026-07-10). `SessionIdManager.checkAndGetSessionId` mints/extends the id and the browser adapter stamps `NeutralEvent.sessionId` on every event (E4-S8). The recording must be tagged with the SAME id events carry. **The recorder reads the shared manager — it does NOT mint its own id**; if it did, recording and events would not stitch. That is the one hard correctness invariant.
- **The linkage must be reachable at enrichment, not just via `getReplayId()`** — architect-locked (epic Notes → success criteria, epic-refine 2026-07-10). A read-only `getReplayId()` that nothing propagates would leave events and recording unjoined. Because the browser target already owns `SessionIdManager` and stamps `NeutralEvent.sessionId` (E4-S8), that shared session id resolves BOTH the recording and the captured events to the same id — so replay and events join on the backend. Assert this equality directly.
- **Re-key on rotation is the one moving part** — architect-locked (epic Notes → "Session/event linkage"). Idle/max-length expiry mints a fresh id mid-recording; the recorder must observe rotation and start a new segment against the new id, or stitching breaks across the rotation (the reference does this via `sessionManager.onSessionId`, `session-recording.ts:235`). This is load-bearing, not gold-plating — an explicit acceptance criterion.
- **Reuse the adapter's existing rotation verdict — do NOT add `onSessionId`** — grounded in the real code: `SessionIdManager` is a pure timestamp-driven mint with no observer; the browser adapter ALREADY detects rotation by comparing the freshly-stamped id against the last one seen (`browser-adapter.ts:639-663`, "Adapter-side session-transition verdict … no onSessionId observer on the pure SessionIdManager"; verdict at `:645-650`, "one rotation-detection mechanism" at `:663`). Hook the recorder's re-key onto that ONE verdict via the `onSessionRotated` fan-out (see Scope.In) — a notification EDGE off the existing decision, not a second detector.
- **CADENCE GAP — the verdict fires only on a CAPTURED EVENT (flagged for user input; do NOT silently paper over).** `classifySessionTransition` runs only inside `runCapturePipeline` (`:585`) — it fires when an analytics event is captured, NOT on a timer. The reference `sessionManager.onSessionId` fires from the session manager's own idle timer (`sessionid.ts`), independent of capture. Because E14's replay uses a SEPARATE delivery path (epic-locked, S4) — rrweb flushes do NOT go through `runCapturePipeline` — a recording that accrues rrweb data while no event happens to be captured will re-key LATE: between an idle/max-length rotation at T and the next captured event at T+N, the recorder still keys data to the OLD session id (mis-attribution for that window). This is a real correctness gap for a CONTINUOUS recorder that the epic did not rule on (the epic-locked "reuse the one verdict / no second detector" is honored; the gap is a different axis — the verdict's FIRING cadence). Three landings: (1) accept capture-gated cadence (defensible ONLY if the recorder relays snapshots through `capture` — but S4's separate delivery path means it does NOT, so this does not close the gap here); (2) give the adapter a lightweight idle-timer TICK that re-runs classify+notify without emitting an event (a driver, not a detector — more faithful to the reference); (3) defer, ship capture-gated, revisit on fidelity complaints. Surfaced to the user at refine — see the refine report's "Concerns for user input." — architect (2026-07-10, refine).
- **De-brand reference:** `posthog-js/packages/browser/src/extensions/replay/session-recording.ts:235` (`sessionManager.onSessionId`) — read for what the reference does on rotation, then adapt to the analytics-kit adapter's existing rotation verdict rather than importing the reference's observer pattern.
- **May fold into S2/S4** if the linkage invariant is asserted within the recorder module rather than as a standalone slice — but the two acceptance criteria (id-equals-events, re-key-on-rotation) MUST be proven regardless. Kept a standalone story because re-key-on-rotation is the epic's one hard correctness invariant and deserves an explicit proof.
- No architect consult needed — every decision above is pre-resolved in the epic `## Notes`.

## Shipped

> Captured by `implement-epics` on 2026-07-10.

- **Files changed:** `ts/packages/browser/src/browser-adapter.ts` (+ `.test.ts`) — `onSessionRotated` + `notifySessionRotated` fired from the existing verdict, `getReplaySessionId` swapped to the shared `lastSeenSessionId` read; `ts/packages/browser/src/replay-recorder.ts` (+ `.test.ts`) — the additive `onRotate?` + `handleRotation` (stop + fresh segment)
- **New public API:** `BrowserAdapter.onSessionRotated(listener): () => void` (adapter-internal — used by the recorder, not on the neutral seam). `ReplayRecorderOptions.onRotate?` (additive). `SessionReplayPort` NOT widened.
- **Tests added:** browser-adapter tests (`onSessionRotated` fires on the one verdict, primes on subscribe, unsubscribes; `getReplaySessionId() === stamped.sessionId`); replay-recorder re-key tests (rotation → `first.stop` once + `startRecording` twice; the subscribe-time prime → exactly ONE `startRecording`, no spurious re-key; `getReplayId` returns the new id post-rotation).
- **Commit:** `main` (message = story title)
- **Reviewer notes:** ship-ready, no critical, first review. All 6 checks confirmed: **one verdict, no second detector** (`notifySessionRotated` fires off the existing `classifySessionTransition` at `runCapturePipeline:596`, peer to pageview/session-entry; `SessionIdManager` diff EMPTY); **`getReplayId` is the SHARED id** (`getReplaySessionId`→`lastSeenSessionId`, recorder never mints — the events↔recording join key); **re-key non-vacuous** (drives the real rotation edge via a fake clock, asserts stop+start, `segmentToken` discards stale async loads, prime is a genuine no-op guarded on `sessionId !== keyedSessionId`); **cadence caveat DOCUMENTED** (capture-gated late re-key in the JSDoc, idle-timer fix deferred, no timer/second-detector sneaked in); **port unwidened + base bundle rrweb-free** (still dynamic `import('./replay')`, S2 bundle test green); neutrality clean. 1 suggestion captured.
- **Cross-story seams exposed:** **S4** — delivers the re-keyed segments: each `start()`/re-key produces a segment keyed to ONE session id (`getReplayId()`), so S4's own delivery path tags snapshots with `getReplayId()` at flush and a rotation cleanly bounds a segment. **Caveat carried:** the capture-gated re-key cadence means a rotation during an event-less stretch re-keys late (v1 ceiling; idle-timer driver deferred). **Reviewer suggestion (2026-07-10):** the `onRotate` listener type is `(string | undefined)` but a rotation notification always carries a DEFINED id (only the subscribe-prime can be `undefined` — `classifySessionTransition` never yields `'rotated'` for an `undefined` id); a one-line note would spare a future reader wondering if a recording can key onto `undefined`. Correct as written; cosmetic.
