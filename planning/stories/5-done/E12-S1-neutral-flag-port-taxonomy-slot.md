---
id: E12-S1-neutral-flag-port-taxonomy-slot
epic: E12-FF-flag-substrate-remote-eval
status: ready-for-dev
area: feature-flags
touches: [core]
depends_on: []
api_impact: additive
---

# E12-S1-neutral-flag-port-taxonomy-slot — Neutral flag-port + taxonomy `flags` slot + bootstrap config (both trees, substrate spike)

## Why

No flag adapter can be written until the neutral port is pinned. This spike lands `FeatureFlagPort<TX>` / `FlagSet` / `FlagContext` / the taxonomy `flags` slot / the `flags.bootstrap` config shape **simultaneously** in both `ports` + `taxonomy` files (TS + Python) so the two trees agree exactly before any per-tree adapter work. It is the one place the trees must converge; everything downstream is per-tree adapter implementation against a frozen contract. **MUST be first** — S2–S6 all build against the shape this story pins.

## Scope

### In

- **TS neutral flag port (`ts/packages/analytics-kit/src/ports.ts`)** — replace the `FeatureFlagPort` sketch (`getFlag(key): unknown`) with the async-first snapshot model, generic over the taxonomy shape:
  - `evaluate(context?: FlagContext): Promise<FlagSet<TX>>` — the ONE load-bearing method (async at the boundary; a `refresh?: boolean` option folds `reload` in — see Technical notes for the reload carve-out).
  - `onChange(listener: (set: FlagSet<TX>) => void): () => void` — change-listener returning an unsubscribe; fires-once-with-resolved-set on server adapters, re-fires on reload in the browser (uniform signature, different cardinality).
  - `FeatureFlagPort<TX extends TaxonomyShape = DefaultTaxonomyShape>` — additive generic with the `DefaultTaxonomyShape` default (mirrors `AnalyticsProvider<TX>`); untyped consumers unaffected.
- **TS `FlagSet<TX>` immutable snapshot type** — sync reads off the resolved snapshot: `isEnabled(key): boolean`, `getFlag(key): FlagValue | undefined`, `getPayload(key): PropsOf<TX['flags'][key]['payload']>` (taxonomy-typed), `getAll(): Record<string, FlagValue>`, plus the **neutral degradation signal**: `degraded: boolean` and `reason(key): FlagReason | undefined`. Define `FlagValue = string | boolean` (variant string / boolean) and the neutral `FlagReason` union **pinned to `'resolved' | 'bootstrap' | 'stale' | 'unresolved'`** (name by consumer-observable state, never a vendor field — see Technical notes; pinned HERE so the three adapters bind to one union, not "confirm at S2"). Vendor eval-quality metadata never reaches this type.
- **TS `FlagContext` type** — `{ distinctId?: string; groups?: Record<string, string>; personProperties?: NeutralProperties; groupProperties?: Record<string, NeutralProperties>; flagKeys?: readonly string[] }`. `distinctId` optional on the type; required-ness on server is adapter-enforced (S3/S4), not a type-level split — the E4 `sessionId` precedent.
- **TS taxonomy `flags` slot (`ts/packages/analytics-kit/src/taxonomy.ts`)** — add a fourth slot to `TaxonomyDecl`: `flags?: Record<string, FlagDecl>` where `FlagDecl = { variants?: readonly string[]; payload?: PropDecl }`. Extend `ShapeOf` / `TaxonomyShape` / `DefaultTaxonomyShape` with a `flags` member so `FlagSet<TX>` reads narrow: `getPayload(key)` → `PropsOf<TX['flags'][key]['payload']>`; `getFlag(key)` narrows to `TX['flags'][key]['variants'][number] | boolean` when variants are declared. Reuse `PropsOf` / `TagToType` verbatim; payloads stay flat-`PropDecl` (nested ⇒ `unknown`).
- **TS bootstrap config (`ts/packages/analytics-kit/src/create-analytics.ts`)** — add `flags?: { bootstrap?: { flags?: Record<string, FlagValue>; payloads?: Record<string, unknown> } }` to `AnalyticsConfig`. Neutral field names (`flags`/`payloads`, never `featureFlags`/`featureFlagPayloads`). Config-shape only in this story — the browser adapter (S2) consumes it.
- **TS canonical empty snapshot — `emptyFlagSet(): FlagSet`** — a seam-level factory returning a frozen "nothing-resolved" `FlagSet` value: `isEnabled()→false`, `getFlag()→undefined`, `getPayload()→undefined`, `getAll()→{}`, `degraded→true`, `reason()→'unresolved'`. This is the `NoopAdapter` null-object precedent applied to `FlagSet` — a real, working, structurally-complete snapshot a consumer can call safely, NOT a bare `undefined`/`{}` that crashes `.isEnabled(...)`. It is the canonical inhabitant of the `FlagSet` contract (the reference value proving `degraded:true`/`reason:'unresolved'` are constructible), so it ships WITH the type here — exactly as `NoopAdapter` shipped with the `AnalyticsAdapter` interface (— architect 2026-07-10). A **factory** (not a single const) because `FlagSet<TX>` is generic. Consumed by S2 (the `'unresolved'` failed-with-no-fallback fallback) and S5 (the absent-`client.flags` case) — both depend only on the seam, so neither forks its own snapshot impl.
- **TS index re-exports** — export the new `FlagSet` / `FlagContext` / `FlagValue` / `FlagReason` / `FlagDecl` types from `ts/packages/analytics-kit/src/index.ts` alongside the already-exported `FeatureFlagPort`, **plus the `emptyFlagSet` value** (re-exported next to `NoopAdapter` — a value export, cross-package-reachable for S5 in `@analytics-kit/react`).
- **Python neutral flag port (`python/src/analytics_kit/ports.py`)** — replace the `FeatureFlagPort` `Protocol` sketch (`get_flag`) with the parity shape: a **synchronous** snapshot `evaluate(context: FlagContext | None = None) -> FlagSet` returning a BARE immutable `FlagSet` (NOT a coroutine — see the "Python `evaluate` is sync-returning by design" note; the blocking round-trip lives inside the adapter, exactly as PY5's query surface hides its async POST/poll behind a blocking `time.sleep`), `on_change(listener) -> Callable[[], None]`, and a `FlagSet` with `is_enabled` / `get_flag` / `get_payload` / `get_all` / `degraded` / `reason`. Server-shaped: `distinct_id` required-ness enforced by the adapter (S4). Mirror the runtime-registry + best-effort-static pattern PY3 locked — no attempt at TS compile-time literal typing. `ports.py` is deliberately OUTSIDE `test_sync_seam.py`'s `_SEAM_MODULES` fence, so a sync `evaluate` there trips nothing — but S4's tests must state it is sync-by-design so a future reader does not "fix" it toward asyncio.
- **Python canonical empty snapshot — `empty_flag_set() -> FlagSet`** — the Python analog of `emptyFlagSet()`, landed in the SAME story for parity (— architect 2026-07-10: don't let parity slip at the substrate story — the one place it's cheapest to keep aligned). Synchronous, returns the immutable Python `FlagSet` with `degraded=True`, `reason → 'unresolved'`, all reads empty. Exported from the Python seam surface (`__init__.py`) alongside the flag types. Consumed by S4's failed-round-trip fallback and any Python consumer adopting without a flag adapter.
- **Python taxonomy `flags` slot (`python/src/analytics_kit/taxonomy.py`)** — add a `flags` key to the `total=False` optional block of `TaxonomyDecl` (the `TypedDict` at `taxonomy.py:108`; a `dict[str, FlagDecl]` runtime shape with `FlagDecl` = variants + payload `PropDecl`), walked by the runtime registry. NOTE Python's `TaxonomyDecl` has NO `page` slot by design (server omission, documented at `taxonomy.py:113-117`) and NO `ShapeOf`/`PropsOf`/mapped-type machinery — the TS `ShapeOf`/`TaxonomyShape` narrowing does not exist here; the Python `flags` slot participates in the runtime registry only (best-effort-static per PY3). Do NOT auto-derive an allowlist from flag payloads (flag payloads are inbound, not consumer-supplied outbound props — leave `derive_allowlist_from_taxonomy` at `taxonomy.py:220` untouched, no `flags` branch).
- **Python bootstrap config (`python/src/analytics_kit/config.py`)** — add the neutral `flags` config field to `AnalyticsConfig` (the Pydantic model at `config.py:18`), Pydantic-validated, parity with the TS shape. Model it as nested Pydantic models (e.g. a `FlagsConfig` with an optional `bootstrap: FlagBootstrap | None`, `FlagBootstrap` = `flags: dict[str, FlagValue] | None` + `payloads: dict[str, object] | None`) so `extra="forbid"` still rejects a typo'd sub-key loudly. Neutral field names (`flags`/`payloads`, never `feature_flags`/`feature_flag_payloads`).
- **Provider-slot type update (both trees)** — retype the already-declared optional `flags?` slot on `AnalyticsProvider` (TS `analytics-provider.ts:55` — bare `FeatureFlagPort`, the seam facade is NOT taxonomy-generic on this slot in R1; retype to `FeatureFlagPort<TX>` ONLY if the frozen-15 pin's `FeatureFlagPort | undefined` assertion is updated in lockstep — see below) and the Python `Analytics` provider's `flags` class attribute (Python `provider.py:76` — the class is named `Analytics`, NOT `AnalyticsProviderImpl`; also re-null'd in `__init__` at `provider.py:94`) from the sketch port to the new `FeatureFlagPort` / `FeatureFlagPort | None`. The slot stays optional and `None`/`undefined`-default — NO adapter is wired here.
  - **Frozen-15 pin (`analytics-provider.test.ts:723-746`) must stay green.** The `keyof AnalyticsProvider` 15-member union pin (`analytics-provider.test.ts:723`) is undisturbed — `flags`/`replay` are already two of the fifteen. But the SEPARATE assertion `expectTypeOf<AnalyticsProvider['flags']>().toEqualTypeOf<FeatureFlagPort | undefined>()` (`analytics-provider.test.ts:744`) pins the slot's exact type; if S1 retypes the seam `flags?` from `FeatureFlagPort` to `FeatureFlagPort<TX>` (or leaves `FeatureFlagPort` but makes the port generic with a `DefaultTaxonomyShape` default), that assertion must be updated in lockstep so it stays exact. Simplest honest shape: keep the seam facade's `flags?: FeatureFlagPort` bare (defaulted generic ⇒ `FeatureFlagPort<DefaultTaxonomyShape>`, which the pin can assert), and carry the taxonomy generic through only where a target actually parameterizes it (browser `RootAnalytics<ShapeOf<T>>` already threads `TX`; node parameterizes its standalone `createFlagClient<T>` factory return `FeatureFlagPort<ShapeOf<T>>` — NOT a `NodeAnalytics` slot; see the flag-attach note below). Confirm the pin's exact expected type when you land the retype.

### Out

- **Any adapter implementation** — browser remote fetch (S2), node remote round-trip (S3), Python server remote (S4). This story wires ZERO live flag evaluation; `evaluate()` has no production caller yet.
- **The React flag hook** — S5.
- **Bootstrap consumption / seeding** — the config FIELD lands here; the browser adapter READS it in S2.
- **Local (in-process) eval** — E13, behind this unchanged `evaluate` method.
- **A recursive JSON-schema `PropType` for nested payloads** — flat-`PropDecl` ceiling for v1 (nested ⇒ `unknown`); noted as a taxonomy-hardening follow-up.
- **`$feature_flag_called` auto-capture / `capture(..., { flags })` coupling** — out of the whole epic.

## Acceptance criteria

- [ ] `FeatureFlagPort<TX>` in `ts/packages/analytics-kit/src/ports.ts` exposes exactly `evaluate(context?): Promise<FlagSet<TX>>` + `onChange(listener): () => void` — and NO separate `reload` (folded into `evaluate({ refresh?: boolean })`); `FlagSet<TX>` exposes `isEnabled` / `getFlag` / `getPayload` / `getAll` / `degraded` / `reason` and nothing vendor-shaped. `FlagReason` = `'resolved' | 'bootstrap' | 'stale' | 'unresolved'`. Generic defaults to `DefaultTaxonomyShape`.
- [ ] The taxonomy `flags` slot narrows reads: a test-only typed taxonomy declaring `flags: { checkout_variant: { variants: ['a','b'], payload: { discount: 'number' } } }` type-checks such that `getFlag('checkout_variant')` narrows to `'a' | 'b' | boolean` and `getPayload('checkout_variant')` narrows to `{ discount: number }` (a `tsd`/`expectTypeOf` type-test as the E3 taxonomy tests do). A nested-payload decl types as `unknown`.
- [ ] `AnalyticsConfig.flags?.bootstrap?.{flags,payloads}` exists with neutral field names; no `featureFlag*` token anywhere on the config surface.
- [ ] `emptyFlagSet(): FlagSet` (TS, re-exported from `index.ts` next to `NoopAdapter`) + `empty_flag_set() -> FlagSet` (Python, exported from `__init__.py`) return a frozen snapshot with `degraded` true, `reason()` → `'unresolved'`, and all reads empty (`isEnabled`→false, `getFlag`/`getPayload`→undefined, `getAll`→{}). A test calls every read on the empty snapshot without throwing. The builder decides (and records in the story's Shipped notes) whether `emptyFlagSet` joins the runtime presence list in `ts/examples/fernly/src/capability-presence.test.ts` — a deliberate choice, not an accident (— architect 2026-07-10).
- [ ] The Python `FeatureFlagPort` Protocol + `FlagSet` mirror the TS surface method-for-method (`evaluate`/`on_change`/`is_enabled`/`get_flag`/`get_payload`/`get_all`/`degraded`/`reason`), with `evaluate` **synchronous** (`def evaluate(...) -> FlagSet`, NOT `async def` — no coroutine, per the PY5 precedent + the locked no-asyncio posture); the taxonomy `flags` slot (added to the `total=False` block of the `TaxonomyDecl` `TypedDict`) + `flags.bootstrap` config field (nested Pydantic models) land, Pydantic-validated; `uv run mypy` / `ruff` / `pytest` green.
- [ ] The `flags` provider slot is retyped to the real port in both trees (TS `AnalyticsProvider.flags?`, Python `Analytics.flags`), stays optional + `None`/`undefined`-default; the frozen-15 `keyof AnalyticsProvider` union pin (`analytics-provider.test.ts:723`) is undisturbed, and the exact-type slot pin (`analytics-provider.test.ts:744`, `AnalyticsProvider['flags']` → `FeatureFlagPort | undefined`) is updated in lockstep with the retype and stays green.
- [ ] Neutrality: `pnpm neutrality-scan` (TS) + the Python scan analog stay green — zero `posthog`/vendor tokens on the port, `FlagSet`, `FlagContext`, taxonomy slot, config field, or any exported name. No wire vocabulary (`$feature*`, `/flags`) appears in the seam (it is adapter-internal, S2+).
- [ ] All gates green both trees: `cd ts && pnpm turbo run build test typecheck lint` + `pnpm neutrality-scan`; `cd python && uv run pytest && uv run ruff check && uv run mypy` + Python scan analog.

## Technical notes

- **Async-first snapshot model — the load-bearing neutrality call (— architect 2026-07-10):** the browser flag read is SYNCHRONOUS in posthog-js (cached in persistence after init fetch); node's is ASYNCHRONOUS (`await evaluateFlags`). Copying the browser's sync shape breaks Python (server-shaped, no persistence/init-fetch) and breaks bar A the moment a self-hosted adapter needs a round-trip. **The neutral read surface is async at the boundary, sync off the snapshot.** PostHog itself converged here — node's per-key `getFeatureFlag`/`isFeatureEnabled`/`getAllFlags` are all `@deprecated` in favor of `evaluateFlags(distinctId, …)` → `FeatureFlagEvaluations` snapshot with sync reads (`posthog-js/packages/node/src/client.ts:1204,1274,1463`; `feature-flag-evaluations.ts`). The neutral port adopts "evaluate once into an immutable snapshot, read synchronously off it." This is a seam decision the PM owns, not a PostHog question — do NOT re-litigate.
- **`reload` carve-out — PINNED to fold-in (— refine):** `reload` folds into `evaluate({ refresh?: boolean })` — NO separate `reload()` method on the port. The port is exactly one async method + `onChange` + the read-only snapshot type, in BOTH trees. Rationale for pinning now (not "decide at S2"): S2/S3/S4 bind to the frozen port, so a late-added `reload` would have to re-propagate to both `ports` files and all three adapters — pin it here so the three adapters can't diverge on whether `reload` exists. A browser fire-and-forget refresh is expressed as `evaluate({ refresh: true })` whose returned promise the caller may ignore; that is sufficient and keeps the neutral surface minimal (bar A). `FlagContext` therefore carries an optional `refresh?: boolean` alongside its evaluation fields — OR `refresh` rides a second `evaluate` options arg; land it as a `refresh?: boolean` member on the options passed to `evaluate` (confirm the exact placement — inside `FlagContext` vs a sibling options bag — when you write S1's type; keep it ONE shape across both trees).
- **`onChange` on a stateless server snapshot (— architect 2026-07-10):** stays on the port, fires-once-with-resolved-set on server (the degenerate case of the browser's re-firing listener — same signature, different cardinality). Rejected: a browser-primary method server adapters omit (an optional-method port weakens the seam + breaks bar-A uniformity). Document the once-semantics as an adapter property (S3/S4 assert it).
- **Neutral degradation signal (— architect 2026-07-10):** `FlagSet` carries `degraded: boolean` + `reason(key): FlagReason | undefined` so a consumer distinguishes "flag is off" from "eval failed / was partial". Without it an eval error collapses silently into a `false`/`undefined` read. The vendor eval-quality metadata that motivates this (`errorsWhileComputing` / `quotaLimited` / `requestId` / PostHog's per-flag `reason` / `locallyEvaluated` on `FeatureFlagEvaluations`) stays **behind the adapter** — only the neutral signal reaches the port. **`FlagReason` is PINNED here to `'resolved' | 'bootstrap' | 'stale' | 'unresolved'`** (refined: no longer "confirm at S2" — the three adapters S2/S3/S4 all bind to ONE union, so it is frozen in S1, not left provisional). Members map to consumer-observable states: `'resolved'` = evaluated fresh from the round-trip/fetch; `'bootstrap'` = served from config bootstrap before any fetch resolved (browser); `'stale'` = a prior cached set served after a failed refresh; `'unresolved'` = no value available (failed with no fallback). If an adapter discovers it needs a member outside this set, the fix comes back to S1 and re-propagates to both trees in the same change — never a per-tree union drift. Named by consumer-observable state, NOT by any vendor field.
- **`FlagContext` asymmetry (— architect 2026-07-10):** `distinctId` optional on the TYPE; **required on the server adapter** (validated — no ambient actor, mirrors node `evaluateFlags(distinctId, …)`), **optional on the browser adapter** (filled from current identity). Same interface, different required-ness enforced by the adapter — exactly how E4 handled `sessionId` (browser stamps it, node leaves it unset). Do NOT invent a fake ambient server actor to force identical signatures.
- **Local-vs-remote is adapter-internal (— architect 2026-07-10):** never a port parameter. `onlyEvaluateLocally`/`strictLocalEvaluation`/poll-interval are adapter config (E13), read by node/Python, ignored by browser. The consumer asks the same question either way; where it's answered is an adapter strategy (exactly how the query client hides HTTP-vs-warehouse behind one `funnel()`).
- **Bootstrap = config, not a method (— architect 2026-07-10):** server-rendered data handed to the client at construction, available synchronously at init before any method could run — a method inverts the timing. Neutral field names strip PostHog's `featureFlag*` prefix. Browser-*primary* (kills a first-paint flash that only exists client-side) but the config field is neutral and Python accepts it too (SSR request-scoped eval) — advances BOTH trees, NOT Python-N/A. The adapter maps it into its backend seeding (browser reference: `posthog-js/packages/browser/src/posthog-featureflags.ts:339` `initialize()`); consumption is S2.
- **Taxonomy `flags` slot (— architect 2026-07-10):** PostHog has no compile-time taxonomy — payloads are untyped `JsonType`, so `defineTaxonomy` is a library-original shape (verified against `ts/packages/analytics-kit/src/taxonomy.ts`: `PropType`/`PropDecl`/`PropsOf`, slots `events`/`traits`/`groups`/`page`). Add `flags` as the fourth slot; reuse `PropsOf` / `TagToType` verbatim. Payload nesting stays flat for v1 (`PropDecl`); nested ⇒ `unknown` — the same pragmatic ceiling `PropsOf` already has. PostHog types `getFlagPayload` as loose `JsonType | undefined`, so a recursive JSON-schema `PropType` is real complexity with no reference driver — REJECTED for v1, noted as a taxonomy-hardening follow-up orthogonal to the FF port. Python mirrors via the PY3 runtime-registry + best-effort-static pattern (a `flags` registry entry, boundary-validated, statically hinted where the checker follows) — the same "not the TS compile-time literal guarantee" ceiling PY3 locked.
- **Carry `TX` through the port generically:** `flags?: FeatureFlagPort<TX>` on `AnalyticsProvider<TX>` — additive generic with `DefaultTaxonomyShape` default, exactly the shipped `AnalyticsProvider<TX = DefaultTaxonomyShape>` pattern. Untyped consumers unaffected.
- **Python `evaluate` is sync-returning by design (— architect 2026-07-10, refine):** Python's `FeatureFlagPort.evaluate` returns a BARE `FlagSet` synchronously — NOT `async def`, NOT a coroutine — even though TS returns `Promise<FlagSet>`. This is the honest per-language parity call, exactly the PY5 query precedent: parity = same capability + same snapshot-read contract (`is_enabled`/`get_flag`/`get_payload`/`get_all`/`degraded`/`reason` on an immutable snapshot), NOT the same async keyword. The blocking round-trip lives inside the Python adapter (S4) as a blocking call, mirroring how PY5's HTTP query adapter hides its async POST/poll behind a blocking `time.sleep`. Making Python `evaluate` a coroutine would introduce the FIRST asyncio surface in the Python tree, contradicting the LOCKED "no asyncio" posture (`test_sync_seam.py`) for zero parity gain. `ports.py` is deliberately outside that fence's `_SEAM_MODULES` (`provider`/`config`/`factory`/`noop`/`adapter`/`client`), so a sync `evaluate` trips nothing — but S4's tests state it is sync-by-design so a reader doesn't "fix" it toward asyncio.
- **Flag-attach surface differs per target — three shapes, ONE port (— refine):** all three targets satisfy the SAME neutral `FeatureFlagPort`; how a consumer OBTAINS it differs by target-surface, and that is a bar-A-preserving detail (bar A = "one adapter, zero consumer change", satisfied by the shared port, not by an identical retrieval path). (1) **Browser (S2)** — `provider.flags` slot: the seam facade `RootAnalytics<ShapeOf<T>>` already carries the optional `flags?` (the frozen-15 member), so the browser adapter attaches there. (2) **Python (S4)** — `Analytics.flags` slot: the single Python provider class `Analytics` carries `flags: FeatureFlagPort | None` (`provider.py:76`), so Python attaches there too. (3) **Node (S3)** — a **standalone `createFlagClient(config)` factory**, NOT a slot: node's consumer surface is the SEPARATE 5-member `NodeAnalytics<TX>` interface (`node-analytics.ts:51`), which is a FROZEN BRIEF surface — `ts/examples/fernly/src/capability-presence.ts:57` (`FrozenNodeMembers`) is a typecheck-time `dist`-export audit that FAILS if a member is renamed, dropped, OR ADDED. So node MUST NOT add a `flags` member to `NodeAnalytics`; it exposes the flag capability the same way it exposes the query capability — a standalone factory mirroring the shipped `createQueryClient` (the query client already established this browser↔node capability-surface asymmetry: browser has no query client, node does; flags is the same story, roles as they fall). **S1 does NOT touch node** (`touches: [core]` correct); this note pins the per-target attach shape so S3 (factory) and S4 (slot) don't diverge from S2 on WHAT they satisfy.
- **`FrozenFlagMembers` capability-presence pin — DECIDED here, APPLIED in S6 (— architect 2026-07-10, refine):** `FeatureFlagPort` is a neutral-seam export (S1 re-exports it from `analytics-kit/index.ts`), so for audit honesty it wants a presence pin in `ts/examples/fernly/src/capability-presence.ts` exactly as `AnalyticsQueryClient` has one (`queryKeys: Equals<keyof AnalyticsQueryClient<…>, FrozenQueryMembers>` at `capability-presence.ts:74` + the Layer-2 return-category pins). S1 DECIDES the frozen member union (it owns the port contract) but does NOT touch the example file (`touches: [core]`): **`FrozenFlagMembers = 'evaluate' | 'onChange'`** (the two port methods; the `FlagSet` snapshot reads are a separate returned type, not port members — pin them via a Layer-2 return-category assertion if desired, e.g. `evaluate` returns `Promise<FlagSet>`). **S6 APPLIES it**: add a `flagKeys: Equals<keyof FeatureFlagPort<DefaultTaxonomyShape>, FrozenFlagMembers>` presence assertion + a Layer-2 `evaluate`-returns-`Promise<FlagSet>` pin to `capability-presence.ts`, importing `FeatureFlagPort` from `analytics-kit`. This is the audit tripwire that keeps the flag capability HONEST on the built `dist` — parallel to how query is pinned. (Recorded here as the decision; the concrete edit is S6's, folded into its capability-presence Scope bullet.)
- **Narrowest test path (— refine):** S1's own gate is TYPE-tests, not runtime behavior (no adapter runs here). TS: an `expectTypeOf`/type-test in the seam package (mirror the E3 taxonomy type-tests + the `analytics-provider.test.ts` frozen-15 pins) proving `getFlag`/`getPayload` narrow off a declared `flags` slot and the `AnalyticsProvider['flags']` slot-type pin stays exact. Python: a `test_seam_substrate.py`/`test_taxonomy.py`-style test asserting the `FeatureFlagPort` Protocol members + `evaluate` is-not-a-coroutine (`inspect.iscoroutinefunction` → False, mirroring `test_sync_seam.py:99`), the `flags` config field parses under `extra="forbid"`, and a typo'd bootstrap sub-key raises.
- **Canonical empty `FlagSet` — `emptyFlagSet()` / `empty_flag_set()` (— architect 2026-07-10):** both S5 (absent `client.flags`) and S2 (failed fetch, no bootstrap/stale fallback → `'unresolved'`) need the SAME neutral "nothing-resolved" snapshot. Hand-rolling it per story drifts — worse, a React-local fake diverges structurally from the adapter's real `'unresolved'` snapshot, a divergence the `capability-presence` dist-audit checks presence/shape of the frozen provider surface but CANNOT see inside. Resolution: a seam-level factory shipped in S1. Grounding: the seam ALREADY exports this shape of thing — `NoopAdapter` (`index.ts:25`) is a re-exported instantiable null-object whose whole job is "a working, structurally-complete impl that resolves to nothing" (`getDistinctId()→'anonymous'`, `getConsentState()→'denied'`, reads→`undefined`). `emptyFlagSet()` is the `FlagSet` analog — same null-object discipline, same bar-B driver (adopt without a backend and still render). It ships WITH the contract exactly as `NoopAdapter` shipped WITH `AnalyticsAdapter` (the seam pointedly did NOT defer the null object to the first real adapter). A **factory, not a const**, because `FlagSet<TX>` is generic (a function returns the correctly-typed empty per taxonomy; a const would force a cast). **Re-exported, not internal**, because S5 lives in `@analytics-kit/react` — a DIFFERENT package — so an internal-only helper isn't cross-package-reachable without either re-export (this) or duplication (the drift). **Lands in S1, not S2:** it's part of the `FlagSet` contract (its canonical inhabitant — `reason:'unresolved'`/`degraded:true` are meaningless as a type without a value that inhabits them), and deferring to S2 would invert the graph (S5/React correctness dragged behind S2/browser for a pure-seam value). Both S2 and S5 then depend ONLY on the seam — the fork is structurally impossible. **Parity:** `empty_flag_set()` lands in the Python seam in the SAME story (sync, frozen, `degraded=True`, `reason→'unresolved'`) — do not let parity slip at the substrate story. **Freeze/neutrality caveats (architect-verified):** zero forbidden tokens (no neutrality tripwire); a new top-level export from `index.ts` does NOT trip `capability-presence.ts`'s `keyof`-of-interfaces freeze (it pins the provider/node/query INTERFACES, not the package export list) — but S1 must change NO frozen `keyof` (the `flags?` slot is already a frozen `AnalyticsProvider` member, so filling its type + adding the `emptyFlagSet` export keeps the key-set unchanged); the one deliberate builder call is whether `emptyFlagSet` joins the runtime presence list in `capability-presence.test.ts` (flagged in the AC above).
- **Coordination boundary (— architect 2026-07-10):** the neutral port + types are decided ONCE, here, in BOTH `ports`+`taxonomy`+`config` files simultaneously, before any adapter work. This is the one place the trees must agree exactly. Do NOT split this epic by language. If S2–S4 discover the port shape is wrong, the fix comes back HERE and re-propagates to both trees — never a per-tree drift. **The exact contract S2/S3/S4 bind to (freeze verbatim):** `evaluate(context?: FlagContext): Promise<FlagSet<TX>>` (TS) / `evaluate(context: FlagContext | None = None) -> FlagSet` sync (Python); `onChange(listener: (set: FlagSet<TX>) => void): () => void` / `on_change(listener) -> Callable[[], None]`; `FlagSet` reads `isEnabled|getFlag|getPayload|getAll` + `degraded: boolean` + `reason(key): FlagReason | undefined`; `FlagValue = string | boolean`; `FlagReason = 'resolved' | 'bootstrap' | 'stale' | 'unresolved'`; `FlagContext = { distinctId?, groups?, personProperties?, groupProperties?, flagKeys? }`. Any adapter that diverges from these signatures is the bug, not the port.
- reference: `posthog-js/packages/core` (shared flag primitives) + `packages/node/src/feature-flag-evaluations.ts` (the snapshot shape to neutralize); de-brand.

> Reviewer suggestion (2026-07-10): Add an exact-shape type pin for `FlagContext` (`expectTypeOf<FlagContext>().toEqualTypeOf<{…}>()`) — `ports.test.ts` only key-pins `keyof FlagContext`, but the field TYPES (`groups: Record<string,string>`, `groupProperties: Record<string, NeutralProperties>`) are load-bearing for S3/S4 and a field-type drift would go uncaught.
> Reviewer suggestion (2026-07-10): Add a Python module-level `FLAG_REASONS: frozenset = {"resolved","bootstrap","stale","unresolved"}` the S4 adapter tests can assert membership against — Python's `FlagReason = str` has no compile-time literal-union protection (unlike TS's literal union), so an off-union `reason` string from an adapter would go uncaught.

## Shipped

> Captured by `implement-epics` on 2026-07-10.

- **Files changed:** `ts/packages/analytics-kit/src/{ports,taxonomy,create-analytics,index}.ts` + tests (`taxonomy.test.ts`, `create-analytics.test.ts`), `ts/examples/fernly/src/capability-presence.test.ts` (`emptyFlagSet` presence pin); `python/src/analytics_kit/{ports,taxonomy,config,__init__}.py`
- **Files added:** `ts/packages/analytics-kit/src/ports.test.ts`, `python/tests/test_flag_port_substrate.py`
- **New public API:** TS — `FeatureFlagPort<TX>`, `FlagSet<TX>`, `FlagContext`, `FlagEvaluateOptions`, `FlagValue`, `FlagReason`, `FlagDecl`, `FlagShape`, `FlagsConfig`, `emptyFlagSet()` (re-exported from `analytics-kit/index.ts`). Python — `FeatureFlagPort`, `FlagSet`, `FlagContext`, `empty_flag_set()`, taxonomy `flags` slot + `FlagsConfig`/`FlagBootstrap` config (via `analytics_kit.__init__`). No adapter wired.
- **Tests added:** TS `ports.test.ts` (port/`FlagSet`/`FlagContext` key-pins + `emptyFlagSet` null-object), `taxonomy.test.ts` (flag-slot narrowing type-tests — reviewer mutation-verified they bite), `create-analytics.test.ts` (bootstrap config), fernly `capability-presence.test.ts` (`emptyFlagSet` presence). Python `test_flag_port_substrate.py` (10: sync `evaluate`, `empty_flag_set` callable/immutable/structural, provider-slot retype None-default, taxonomy slot walkable, payloads NOT allowlist-derived).
- **Commit:** `main` (message = story title)
- **Reviewer notes:** ship-ready, no critical, first review. Reviewer mutation-tested two type-tests (narrowing genuinely enforced) + confirmed all PostHog eval-quality metadata (`errorsWhileComputing`/`quotaLimited`/`requestId`/per-flag `reason`/`locallyEvaluated`) is excluded from the neutral `FlagSet`. 2 suggestions captured above → improvement-pass seeds.
- **Cross-story seams exposed:** the frozen contract S2/S3/S4/S5 bind to is pinned in `ports.*` (verbatim in the "Coordination boundary" note): `evaluate(context?, options?): Promise<FlagSet<TX>>` (TS) / sync Python; `onChange`; `FlagSet` reads + `degraded`/`reason`; `FlagReason='resolved'|'bootstrap'|'stale'|'unresolved'`; `FlagContext` 5 fields. **Attach differs per target:** browser (S2) + Python (S4) via the `provider.flags` slot; node (S3) via a standalone `createFlagClient` factory (NOT a `NodeAnalytics` slot — `FrozenNodeMembers` is frozen). `emptyFlagSet()`/`empty_flag_set()` is the single-sourced null-object S2+S5 both consume. `FrozenFlagMembers = 'evaluate'|'onChange'` DECIDED here; S6 APPLIES the `capability-presence.ts` pin.
